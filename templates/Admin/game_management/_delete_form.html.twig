<form method="post" action="{{ path('app_game_management_delete', {'id': game_management.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game_management.id) }}">
    <button class="btn">Delete</button>
</form>

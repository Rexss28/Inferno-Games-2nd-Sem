<form method="post" action="{{ path('app_license_key_delete', {'id': license_key.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ license_key.id) }}">
    <button class="btn">Delete</button>
</form>
